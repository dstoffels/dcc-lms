'use client';

import { createTheme } from '@mui/material/styles';

const theme = createTheme({
	palette: {
		mode: 'dark',
		secondary: {
			main: '#1E313B',
		},
		background: {
			// default: '#000',
		},
	},
});

export default theme;
